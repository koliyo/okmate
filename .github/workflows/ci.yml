name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Checkout h35-desktop sibling
        run: git clone --depth 1 https://github.com/koliyo/h35-desktop.git "${GITHUB_WORKSPACE}/../h35-desktop"
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all -- --check
      - name: Test engine and app
        run: cargo test -p okf && cargo test -p okmate --no-default-features
      - name: Check knowledge bundle
        run: cargo run -q --no-default-features -p okmate -- check knowledge --profile strict --format terminal
      - uses: astral-sh/setup-uv@v6
      - name: Test okmate-ops
        run: uv run --directory tools/okmate-ops --group dev pytest
